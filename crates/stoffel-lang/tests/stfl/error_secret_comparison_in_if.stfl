def main() -> None:
  var threshold: int64 = 10
  var secret_age: secret int64 = 12
  var s: secret int64 = 0

  # This should fail semantic analysis (no branching on secrets)
  if secret_age > threshold:
    discard s

  discard threshold
  discard s
