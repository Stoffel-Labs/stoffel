# FizzBuzz sample that computes a checksum for VM testing
# It iterates 1..100 and accumulates a checksum:
# +3 for FizzBuzz (divisible by 15), +1 for Fizz (divisible by 3), +2 for Buzz (divisible by 5)
# Expected checksum: 126

def main() -> None:
  var sum = 0

  for i in 1..100:
    if (i % 15) == 0:
      sum = sum + 3
    else:
      if (i % 3) == 0:
        sum = sum + 1
      else:
        if (i % 5) == 0:
          sum = sum + 2
        else:
          discard i

  discard sum
