# Invalid example: branching on a secret value (should cause a compiler error)

# A function that produces a secret boolean
def secret_flag() -> secret bool:
  return True

def main() -> None:
  var cond: secret bool = secret_flag()

  # This is intentionally wrong: using a secret value as an if-condition
  if cond:
    print("should not reach here")
  else:
    print("nor here")

  while cond:
    print("test")
