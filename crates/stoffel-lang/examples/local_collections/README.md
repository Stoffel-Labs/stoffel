# Local Collections

Demonstrates typed list literals and the method-style `append`, `push`,
`length`, and `len` aliases. This example does not require MPC and is run by the
validation script through `stoffel-run`.
