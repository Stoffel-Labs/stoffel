# Language MPC Schemas

This example defines public object schemas with secret-typed fields that describe an MPC job
request, constructs typed runtime objects with named field initializers, then builds nested
review metadata with `list[list[int64]]` and `dict[string, list[int64]]`.
