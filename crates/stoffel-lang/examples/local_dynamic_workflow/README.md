# Local Dynamic Workflow

This example builds a small in-memory work item with dynamic object fields,
array builtins, runtime type inspection, an inferred-return helper, and a
closure callback. It is useful for host-integrated code that needs to assemble
VM-native objects and invoke a named handler dynamically.
