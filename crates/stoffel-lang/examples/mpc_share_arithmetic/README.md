# MPC Share Arithmetic

Demonstrates clear-to-share conversion, secret-share arithmetic, and opening the
result. Run this with `examples/docker-compose.mpc.yml` or
`./examples/validate_examples.sh --docker-mpc`.

This example intentionally exercises share multiplication, so it is heavier than
the default 5-party smoke test.
