# Threshold ECDSA over secp256k1

Threshold ECDSA example adapted from the StoffelVM fixture. It returns
`r(32) || s(32) || SEC1 compressed public key(33)`.

Run with AVSS and `STOFFEL_MPC_CURVE=secp256k1`.
