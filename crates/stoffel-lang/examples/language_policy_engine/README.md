# Language Policy Engine

This example is a small rule-scoring engine for deciding whether a payout can be
auto-approved. It demonstrates imports, aliases, typed functions, type aliases,
 numeric literal widths, branching, loops, range iteration, list and dictionary
literals, indexing, string iteration, unary operators, boolean operators, and
compound assignment.
